# base directory
base.dir <- ""

# number of barcodes required
desired.barcodes <- 96

# max iterations to try and achieve required barcode number
iter.number <- 10

# desired length of barcodes
barcode.length <- 6

# will ensure barcodes have at least this number of mismatches
n.dist <- 2

# maximum number of barcodes allowed to have a matching 3-mer in the same position
max.nmer <- 6

# maximum fraction of barcodes allowed to have the same base in the same position
max.base.frac <- 0.35
